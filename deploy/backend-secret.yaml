apiVersion: v1
kind: Secret
metadata:
  name: ohorizons-tasks-api-db
type: Opaque
stringData:
  # Reemplazar antes de desplegar (o sincronizar vía External Secrets desde Key Vault).
  DATABASE_URL: postgres://appuser:CHANGE_ME@postgres:5432/appdb
